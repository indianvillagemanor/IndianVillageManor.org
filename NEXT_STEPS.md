test IVM.org backup of db and uploaded files
downloads of newsletters -- filenames are being prefixed with a hexadecimal followed by an underscore.  let's remove that prefix when downloading
come up with a manual testing suite for a few difficult-to-automate features, inc: not sure if downloads are being tracked correctly, server redeploy with new code
